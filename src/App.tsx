import { FC } from 'react';
import { Button } from './components';
import './i18n';

const App: FC = () => {
  return (
    <div className="App">
      <Button>CLick</Button>
    </div>
  );
};

export default App;
